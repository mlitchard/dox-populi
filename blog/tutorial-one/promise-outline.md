Title: Claude, I'm in Trouble. (yours, set)

1. The record contract. What every numbered post stands on:
  a branch to check out, a transcript to read. This page has neither for its step.
  - Must keep: both artifacts named; section 1 predates the repo's history.
2. The each-branch workflow. git checkout, download iso, run-vm.sh;
   it should have existed on this branch from the start and didn't —
   the vm machinery in this post is that retrofit.
   (Temptation/turn beat CUT from the Promise: the Session dramatizes
   it. Its handoff line — the session below is the repair happening —
   closes the Promise, his placement.)
4. Where the baseline came from. One sentence.
  - Must keep: earliest commit = period artifact;
    baseline = that commit minus two lines
    (upgrader, builder — declared, no behavior).
5. What you'll learn. The three objectives (lesson-plan law:
   every postscript exercise maps here).
  - Must keep: smallest state of the apparatus / what each check rejects
  - (Paradox rejects an inconsistent spec and generates the decision logic
    the harness imports; tsc makes the mismatch a compile error;
    the build bundles) / deliver and watch.                                                                          6. Side by side. Official role.has role branch.
  - Must keep: the slice caveat — the full loop adds auto-spawn and memory
    reclaim because nobody sits at a console; the role and body come from
    the spec; Paradox generates what the harness imports.
7. Equipment. QEMU + tmux host, the stage ISO, the branch inside the VM.
   Must keep: the honesty sentence on a nix host; the installer removing that
   requirement came later; this ISO is that fix applied backward.
   Beat 6 is where the current draft sounds most like me — it carries
   the most rewriting room. The "must keep" every word around them is yours to replace.
