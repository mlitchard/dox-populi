# Nodes backlog

Candidate nodes to build out later, each its own directory when it
happens. His notes, moved here from the fast-path promise draft.

## Deeper-dive nodes (particulars)

Shallow or deeper dives into the particulars. Delivery priority favors
the flake infrastructure first, then:

- node(s) on the dox spec code itself;
- a node on the harness;
- stretch goal: how to build your own gitlab runner.

## Edge note: dig links on the objectives

The fast-path promise's "What you'll learn" list is a good place for
the deeper-dive (dig) links — each objective points to its node
(boot/infra → the flake node, spec → the dox node, harness → the
harness node) once those nodes exist.
