We settled the assignment structure for the tutorial course material in an earlier session. Each tutorial section gets a four-part assignment:

1. Predict — before running anything, the student reads the section's tutorial JS and writes down what the creeps will do and how long it will keep working.
2. Witness — run it, watch until it breaks (each tutorial section has a built-in failure the next section fixes), compare against the prediction. The observed failure becomes the opening of the next section's assignment.
3. Sort — the student sorts every statement of the section's JS into two columns: decision (role choice, thresholds, when to switch tasks) or action (Screeps API calls). They check the sort against the matching bion column should account fordox/creeps.dox, the action column for shell/main.ts. The repo already did the port and serves as the answer key. Wheree repo, they explain which oneis wrong.                                                                                    4. Bounded spec edit — the assic spec change that violates anamed invariant. They make it, run paradox check, read the rejection, then write the version that passes. The change is chosed to have something to say.

Reference tutorial JS lives at s/tutorial-scripts. Branch names like 1-tutorial-one track tutorial sections. The "in the trenches" section I already started is the primary structure for these assigments.

Task: draft the assignment for tutorial section 1 in this structure.
