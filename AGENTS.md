# dox-populi

Follow `CLAUDE.md`.

Subagent types live in `.claude/agents/`:

- `spec-author` — `dox/creeps.dox`
- `shell-hands` — `shell/main.ts`
- `tutorial-porter` — next tutorial section
- `nix-pipeline` — flake, vendoring, VM, CI
- `server-doctor` — server, deploy, harvest
