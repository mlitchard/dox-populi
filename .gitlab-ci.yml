# =============================================================================
# GitLab CI Configuration
# Generated by: nix run .#gitlab-ci
# DO NOT EDIT MANUALLY - regenerate with the command above
# =============================================================================

apps:cli:
  needs:
    - packages:screeps-server
  script:
    - nix build .#apps.x86_64-linux.cli.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.cli.program))')"
  stage: build
apps:client:
  needs:
    - packages:screeps-client
  script:
    - nix build .#apps.x86_64-linux.client.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.client.program))')"
  stage: build
apps:deploy:
  needs:
    - apps:secrix
    - packages:default
    - packages:generated
    - packages:secrix
  script:
    - nix build .#apps.x86_64-linux.deploy.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.deploy.program))')"
  stage: build
apps:deploy-local:
  needs:
    - apps:secrix
    - packages:default
    - packages:generated
    - packages:secrix
  script:
    - nix build .#apps.x86_64-linux.deploy-local.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.deploy-local.program))')"
  stage: build
apps:generate:
  needs:
    - packages:generated
  script:
    - nix build .#apps.x86_64-linux.generate.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.generate.program))')"
  stage: build
apps:gitlab-ci:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.gitlab-ci.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.gitlab-ci.program))')"
  stage: build
apps:itest:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.itest.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.itest.program))')"
  stage: build
apps:lock-client:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.lock-client.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.lock-client.program))')"
  stage: build
apps:lock-mods:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.lock-mods.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.lock-mods.program))')"
  stage: build
apps:lock-server:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.lock-server.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.lock-server.program))')"
  stage: build
apps:reset-local:
  needs:
    - packages:screeps-server
  script:
    - nix build .#apps.x86_64-linux.reset-local.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.reset-local.program))')"
  stage: build
apps:secrix:
  needs: []
  script:
    - nix build .#apps.x86_64-linux.secrix.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.secrix.program))')"
  stage: build
apps:server:
  needs:
    - apps:secrix
    - packages:screeps-server
    - packages:secrix
  script:
    - nix build .#apps.x86_64-linux.server.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.server.program))')"
  stage: build
apps:stop:
  needs:
    - packages:screeps-server
  script:
    - nix build .#apps.x86_64-linux.stop.program --no-link --print-build-logs 2>/dev/null || nix build --no-link --print-build-logs "$(nix eval --raw --impure --expr 'builtins.head (builtins.attrNames (builtins.getContext (builtins.getFlake (toString ./.)).apps.x86_64-linux.stop.program))')"
  stage: build
checks:build:
  needs:
    - packages:default
    - packages:generated
  script:
    - nix build .#checks.x86_64-linux.build --no-link --print-build-logs
  stage: test
checks:fsm-behavior:
  needs:
    - packages:generated
  script:
    - nix build .#checks.x86_64-linux.fsm-behavior --no-link --print-build-logs
  stage: test
checks:itest:
  needs:
    - apps:deploy-local
    - apps:secrix
    - apps:server
    - packages:default
    - packages:generated
    - packages:screeps-server
    - packages:secrix
  script:
    - nix build .#checks.x86_64-linux.itest --no-link --print-build-logs
  stage: test
checks:paradox-check:
  needs: []
  script:
    - nix build .#checks.x86_64-linux.paradox-check --no-link --print-build-logs
  stage: test
checks:typecheck:
  needs:
    - packages:generated
  script:
    - nix build .#checks.x86_64-linux.typecheck --no-link --print-build-logs
  stage: test
checks:vm-boot:
  needs: []
  script:
    - nix build .#checks.x86_64-linux.vm-boot --no-link --print-build-logs
  stage: test
devShells:default:
  needs:
    - apps:secrix
    - packages:secrix
  script:
    - nix build .#devShells.x86_64-linux.default --no-link --print-build-logs
  stage: build
flake:check:
  script:
    - nix flake check --print-build-logs
  stage: validate
flake:show:
  needs: []
  script:
    - nix flake show --allow-import-from-derivation
  stage: build
gitlab-ci:check:
  needs: []
  script:
    - set -xeo pipefail
    - nix run .#gitlab-ci | tee .gitlab-ci.yml.new
    - echo "wrote .gitlab-ci.yml.new"
    - 'diff -u .gitlab-ci.yml .gitlab-ci.yml.new || (echo ''ERROR: .gitlab-ci.yml is out of date. Run: nix run .#gitlab-ci > .gitlab-ci.yml'' && exit 1)'
  stage: validate
nixosConfigurations:dox-populi:
  needs: []
  script:
    - nixos-rebuild build --flake .#dox-populi -L
  stage: build
nixosConfigurations:installer:
  needs: []
  script:
    - nixos-rebuild build --flake .#installer -L
  stage: build
nixosConfigurations:vm:
  needs: []
  script:
    - nixos-rebuild build --flake .#vm -L
  stage: build
packages:default:
  needs:
    - packages:generated
  script:
    - nix build .#packages.x86_64-linux.default --no-link --print-build-logs
  stage: build
packages:generated:
  needs: []
  script:
    - nix build .#packages.x86_64-linux.generated --no-link --print-build-logs
  stage: build
packages:installer-iso:
  needs:
    - nixosConfigurations:installer
  script:
    - nix build .#packages.x86_64-linux.installer-iso --no-link --print-build-logs
  stage: build
packages:main:
  needs:
    - packages:default
    - packages:generated
  script:
    - nix build .#packages.x86_64-linux.main --no-link --print-build-logs
  stage: build
packages:screeps-client:
  needs: []
  script:
    - nix build .#packages.x86_64-linux.screeps-client --no-link --print-build-logs
  stage: build
packages:screeps-server:
  needs: []
  script:
    - nix build .#packages.x86_64-linux.screeps-server --no-link --print-build-logs
  stage: build
packages:secrix:
  needs:
    - apps:secrix
  script:
    - nix build .#packages.x86_64-linux.secrix --no-link --print-build-logs
  stage: build
stages:
  - build
  - test
  - validate
variables:
  NIX_CONFIG: experimental-features = nix-command flakes
